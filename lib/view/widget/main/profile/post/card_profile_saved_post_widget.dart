import 'package:flutter/material.dart';
import 'package:stagemuse/utils/export_utils.dart';

class CardProfileSavedPostWidget extends StatelessWidget {
  const CardProfileSavedPostWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset(gambarSignUp, fit: BoxFit.cover);
  }
}
