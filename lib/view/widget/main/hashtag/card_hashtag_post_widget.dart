import 'package:flutter/material.dart';
import 'package:stagemuse/utils/export_utils.dart';

class CardHashtagPostWidget extends StatelessWidget {
  const CardHashtagPostWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset(gambarSignUp, fit: BoxFit.cover);
  }
}
